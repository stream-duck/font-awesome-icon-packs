<div class="bg-gray-800 rounded-xl shadow-lg p-4 hover:scale-105 transition">
  <img src="{{ preview }}" alt="{{ name }}" class="rounded mb-3 object-cover w-full h-40">
  <h3 class="text-lg font-semibold text-white">{{ name }}</h3>
  <p class="text-sm text-gray-400">{{ version }} • {{ variant }} • Background: {{has_background}}</p>
  <a href="{{ download }}" class="mt-3 inline-block w-full text-center bg-blue-600 hover:bg-blue-700 text-white py-2 rounded">
    Download
  </a>
</div>